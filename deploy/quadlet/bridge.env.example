# Install as /etc/termoso/bridge.env (mode 0600) when termoso-bridge.container
# is enabled. Callers must present the key as `Authorization: Bearer <key>`
# or `X-Api-Key`. Generate one with: openssl rand -hex 32
TERMOSO_BRIDGE_API_KEY=
TERMOSO_BRIDGE_SYNC_INTERVAL=60
RUST_LOG=info
